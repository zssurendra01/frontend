pipeline {
    agent {
        label "node1"
    }
    stages {
        stage('Download ') {
            steps {
                sh '''
                 npm install
                 npm run build
                '''
            }
        }
        stage('seeing artifacts') {
            steps {
                sh '''
                zip -r frontend.zip *
                '''
            }
        }
 
    }
} 