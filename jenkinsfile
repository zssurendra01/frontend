pipeline{
    agent any
    stages{
        stage("seeing artifacts"){
            steps{
                sh '''
                cd static
                zip -r frontend.zip *
                '''
            }
        }
 
}
}