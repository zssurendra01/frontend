pipeline {
    agent any
    stages {
        
        stage("seeing artifacts") {
            steps {
                sh '''
                zip -r ../frontend.zip *
                '''
            }
        }
 
    }
}