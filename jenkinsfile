pipeline {
    agent {
        label "node1"
    }
    stages {
        stage('seeing artifacts') {
            steps {
                sh '''
                zip -r ../frontend.zip *
                '''
            }
        }
 
    }
} 